# Netlify config (root) — build from frontend directory
[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


