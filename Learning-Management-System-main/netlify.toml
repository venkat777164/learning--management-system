# Netlify config (root) — build from frontend directory
[build]
  command = "npm --prefix Learning-Management-System-main/frontend install && npm --prefix Learning-Management-System-main/frontend run build"
  publish = "Learning-Management-System-main/frontend/build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


